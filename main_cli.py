"""
main_cli.py — CLI MVP Launcher (for grading)

Runs the integrated CLI MVP (engine_cli.py).
Pygame visual demo remains the default at: py main.py
"""

from engine_cli import run_cli

if __name__ == "__main__":
    run_cli()
